# LLM integration
# Floor pin (not exact): a global langchain install may require a newer
# anthropic, and the tool-use API used here is stable across these versions.
anthropic>=0.39.0
python-dotenv==1.0.1

# File parsing
pdfplumber==0.11.4
pypdf==4.3.1
python-docx==1.1.2

# Sample-data generation (scripts/make_samples.py)
fpdf2==2.7.9

# Testing
pytest==8.3.3
